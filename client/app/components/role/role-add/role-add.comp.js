import template from './role-add.html';
import controller from './role-add.ctrl';
import './role-add.less';

let Component = {
    restrict: 'E',
    bindings: {},
    template,
    controller,
    controllerAs: 'vm'
};

export default Component;