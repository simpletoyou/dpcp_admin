import template from './locketlist.html';
import controller from './locketlist.ctrl';
import './locketlist.less';

let Component = {
    restrict: 'E',
    bindings: {},
    template,
    controller,
    controllerAs: 'vm'
};

export default Component;
